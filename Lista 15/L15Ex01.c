#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>


#include "pessoa.h"

#define TAM 10

int main(){
    PESSOA cadastro[TAM];
    int i=0;
    do{
        cadastro[i] = (PESSOA*)malloc(sizeof(PESSOA));
        i++;
    }while(flag)
    
}